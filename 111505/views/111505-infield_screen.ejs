<!DOCTYPE html>
<html>
    <head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, 
      initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
      <title>內場畫面</title>
    </head>

    <style>
      body{
        width: 100%;
        height: 100vh;
        overflow: hidden;
        background-image: url(background2.jpg);
        background-size: cover;
        background-position: center;
        background-repeat: no-repeat;
        
      }

      table{
        position: absolute;
        border-collapse: collapse;
        position: absolute;
        top: 10%;
        top: 16.3%;
        left: 30%;
        right: 30%;
      }

      table, th, td{
        border: 5px  solid black;
      }

      tr, th, td{
        width: 400px;
        font-weight:bold;
        font-family: DFKai-sb;
      }

      th , tr{
        border: 5px  solid black;
      }

      th{
        font-size: 60px;
        background-color: rgba(166, 166, 166);
      }
      
      td{
        text-align: center;
        font-size: 40px;
        background-color: rgba(166, 166, 166);
      }

      #img1{
        width: 15%;
      }

      #img2{
        position: absolute;
        width: 15%;
        top: 0%;
        right: 0%;
      }

      #img3{
        position: absolute;
        width: 15%;
        bottom: 0%;
        left: 0%;
      }

      #img4{
        position: absolute;
        width: 15%;
        bottom: 0%;
        right: 0%;
      }

      @media only screen and (max-width:1300px){
        table, th, td{
        border: 5px solid rgb(134, 6, 6);
      }
      }

      @media only screen and (max-width:1200px){
        table{
        position: absolute;
        border-collapse: collapse;
        position: absolute;
        top: 10%;
        left: 30%;
        right: 30%;
        }
      }

      .lineHeightmin{
        color: rgb(156, 2, 2);
      }
    </style>

    <body>
      <table>
        <tr>
          <th class = lineHeight>壽司名稱</th>
          <th class = lindeHeight>需補數量</th>
        </tr>
          <% for(var i=0 ; i<data.length ; i++){ %>
            <tr>
              <% if (10-data[i].Quantity > 7) { %>
                <td  class = lineHeightmin><%= data[i].Chinese_Name%></td>
                <td  class = lineHeightmin><%= 10-data[i].Quantity%></td>
              <%}else if (10-data[i].Quantity > 0){ %>
                <td  class = lineHeight><%= data[i].Chinese_Name%></td>
                <td  class = lineHeight><%= 10-data[i].Quantity%></td>
              <%} %> 
            </tr>
          <% } %>
      </table>

      <script language="JavaScript">
        function myrefresh()
        {
        window.location.reload();
        }
        setTimeout('myrefresh()',1000);
      </script>
      </table>
    </body>
</html>